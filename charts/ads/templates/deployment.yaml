apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ .Values.namespace }}
  name: {{ include "ads.fullname" . }}
  labels:
    {{- include "ads.labels" . | nindent 4 }}
    app.kubernetes.io/component: ads
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "ads.adsSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ads.adsSelectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "ads.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: ads
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: https
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          {{- if .Values.kafka.existingSecret }}
          env:
            - name: ADS_KAFKA_SASL_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.kafka.existingSecret }}
                  key: username
            - name: ADS_KAFKA_SASL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.kafka.existingSecret }}
                  key: password
          {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "ads.fullname" . }}
            - secretRef:
                name: {{ include "ads.fullname" . }}
          volumeMounts:
            {{- if .Values.kafka.caBundle.secretName }}
            - name: kafka-ca
              mountPath: /kafka-ca
              readOnly: true
            {{- end }}
            - name: tls
              mountPath: /certs
              readOnly: true
            {{- if .Values.tls.caBundle.secretName }}
            - name: ca
              mountPath: /ca
              readOnly: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health/live
              port: https
              scheme: HTTPS
            initialDelaySeconds: 10
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health/ready
              port: https
              scheme: HTTPS
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        {{- if .Values.kafka.caBundle.secretName }}
        - name: kafka-ca
          secret:
            secretName: {{ .Values.kafka.caBundle.secretName }}
            items:
              - key: {{ .Values.kafka.caBundle.key | default "ca.crt" }}
                path: ca.crt
        {{- end }}
        - name: tls
          secret:
            secretName: {{ include "ads.serviceSecretName" . }}
        {{- if .Values.tls.caBundle.secretName }}
        - name: ca
          secret:
            secretName: {{ .Values.tls.caBundle.secretName }}
            items:
              - key: {{ .Values.tls.caBundle.key | default "ca.crt" }}
                path: ca.crt
        {{- end }}
      nodeSelector:
        {{- include "ads.applicationNodeSelector" . | nindent 8 }}
        {{- with .Values.nodeSelector }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
