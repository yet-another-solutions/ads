[project]
name = "ads-egress-controlplane"
version = "0.0.1"
description = "ADS egress control plane"
requires-python = ">=3.12"
authors = [{ name = "yet-another-solutions" }]
dependencies = []

[project.scripts]
ads-egress-controlplane = "ads_egress_controlplane.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ads_egress_controlplane"]
